// app.js — minimal global initialization
// Most page logic is handled by page-specific scripts (dashboard.js, expenses.js, etc.)
// This file is only loaded on pages that include it.

window.app = window.app || {};
